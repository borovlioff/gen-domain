// application/useCases/deleteManyEntities/DeleteManyEntities.ts
import { <%= entity %>Repository } from "../../domain/repositories/<%= entity %>Repository";

export class DeleteMany<%= entity %> {
  constructor(private repository: <%= entity %>Repository) {}

  async execute(filter: Partial<<%= entity %>>): Promise<{ deletedCount: number }> {
    return this.repository.deleteMany(filter);
  }
}