import { <%= entity %>Repository } from "../../domain/repositories/<%= entity %>Repository";

export class BulkWriteEntities {
  constructor(private repository: <%= entity %>Repository) {}

  async execute(operations: any[]): Promise<{ success: boolean }> {
    return this.repository.bulkWrite(operations);
  }
}